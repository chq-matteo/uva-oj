from sys import stdin, stdout
t = int(stdin.readline())
stdin.readline()
def mask(d, l0, l1)
for i in range(t):
    q = stdin.readline().split()
    if len(q) == 2:
        a, b = (int(x) for x in q)
        # due soli
    else:
        if q[0] == '*':
            # tutti
        else:
            # uno preciso