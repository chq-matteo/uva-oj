#include <iostream>
#include <sstream>
#include <algorithm>
using namespace std;
int main() {
    int t;
    cin >> t;
    string s;
    istringstream iss;
    getline(cin);
    for (int i = 0; i < t; i++) {
        s = getline(cin);
        iss = {s};
        if (s[0] == '*') {

        }
        
    }
}